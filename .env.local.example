# Google Drive API key used to live-fetch new photos the client uploads to
# the People Drive folders (folders 2 & 3) — see src/lib/drive.ts.
# Restrict this key in Google Cloud Console to: Google Drive API only,
# HTTP referrers = your site's domains (+ localhost for dev).
NEXT_PUBLIC_GOOGLE_DRIVE_API_KEY=
